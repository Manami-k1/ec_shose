import { BaseDivider, BaseFlexBox, BaseHeader } from "@/components";
import BaseButton from "@/components/BaseButton";
import BaseKeyVisual from "@/components/BaseKeyVisual";

const Home = () => {
  return (
    <>
      <BaseHeader />
      <BaseKeyVisual />
    </>
  );
};
export default Home;
